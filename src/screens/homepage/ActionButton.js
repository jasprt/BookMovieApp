import { Button } from '@material-ui/core';
import React from 'react';
import './ActionButton.css'

const ActionButton = () => {

    return (
        <Button variant="contained" color="default" className="action-btn">Login</Button>
    )
}

export default ActionButton;